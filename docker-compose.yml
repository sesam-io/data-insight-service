version: '3'
services:
  insight:
    build: .
    image: ${DOCKER_USER}/insight:${DOCKER_TAG}
    ports:
      - "5000:5000"
    environment:
      URL:  ${DEV_URL}/datasets
      LOG_LEVEL: "DEBUG"
      JWT: ${JWT}
